meta {
  name: get_home-page
  type: http
  seq: 1
}

get {
  url: http://localhost:5000/
  body: none
  auth: none
}

tests {
  test("home page is correctly opened", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
