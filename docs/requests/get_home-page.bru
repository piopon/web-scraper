meta {
  name: get_home-page
  type: http
  seq: 1
}

get {
  url: http://localhost:5000/
  body: none
  auth: none
}

tests {
  test("home page is correctly opened", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("home page received HTML data", function() {
    expect(res.getHeader("content-type")).to.equal("text/html; charset=utf-8");
  });

  test("home page has correct content length", function() {
    expect(res.getHeader("content-length")).to.equal("3116");
  });

  test("home page has login data", function() {
    const body = res.getBody();
    expect(body).to.include('<h3>scraper user login</h3>');
    expect(body).to.include('<form action="/auth/login" method="POST">');
    expect(body).to.include('<input type="email" id="user-email" name="email" required>');
    expect(body).to.include('<input type="password" id="user-password" name="password" required>');
  });
}
